<html>
<head>
  <title><%= @album.title %> - The Warheads</title>
</head>
<body>
  <h1><%= @album.title %></h1>

  <p>Released <%= @album.month_name %> <%= @album.day %>, <%= @album.year %></p>

  <% if @album.description -%>
    <p><%= @album.description %></p>
  <% end -%>

  <h2>Track listing</h2>

  <table>
    <tr>
      <th>Title</th>
      <th>Time</th>
    </tr>
    <% @album.tracks.each do |track| -%>
    <tr>
      <td><%= track.fetch("title") %></td>
      <td><%= track.fetch("time") { "" } %></td>
    </tr>
    <% end -%>
  </table>
</body>
</html>
